# macOS mouse tuning for The Terminator: Future Shock — replaces Wine+SkyNETMouseHelper.exe
# raw_mouse_input bypasses macOS acceleration for consistent FPS mouse look.
[mouse]
mouse_capture        = onclick
raw_mouse_input      = true
