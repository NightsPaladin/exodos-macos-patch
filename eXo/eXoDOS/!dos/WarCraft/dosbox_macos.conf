# macOS mouse tuning for Warcraft: Orcs & Humans — replaces Wine+WarcraftMouseHelper.exe
# raw_mouse_input bypasses macOS acceleration; sensitivity tuned for RTS cursor control.
[mouse]
mouse_capture        = onclick
raw_mouse_input      = true
mouse_sensitivity    = 50
